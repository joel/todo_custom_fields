<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">New item</h1>

  <%= turbo_frame_tag "new_item" do %>
    <%= render "form", item: @item %>
  <% end %>

  <%= link_to 'Back to items', todo_items_path(@todo), class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
</div>
