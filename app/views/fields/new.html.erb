<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">New Custom Field</h1>

  <%= turbo_frame_tag "new_field" do %>
    <%= render "form", field: @field %>
  <% end %>

  <%= link_to 'Back to todo', todo_path(@todo), class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
</div>
